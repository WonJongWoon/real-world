package com.realworld.user.adapter.out.persistence

import com.realworld.user.application.port.`in`.RegistrationCommand

class UserMapper {
    fun mapToUser(registrationCommand: RegistrationCommand) {
        TODO()
    }
}
